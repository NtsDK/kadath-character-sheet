import { observer } from "mobx-react-lite";
import { Button } from "antd";

import { getLibraryPageStore } from "../IoC";

export const GameCatalogPage = observer(() => {
  return <div>GameCatalogPage</div>
});
